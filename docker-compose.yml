services:
  website:
    build: ./website
    volumes:
      - ./db:/db
    ports:
      - "3002:3002"
    command: ["deno", "run", "--allow-all", "server.ts"]

  cms:
    build: ./cms
    volumes:
      - ./db:/db
    ports:
      - "3003:3003"
    command: ["deno", "run", "--allow-all", "admin.ts"]


